[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx next build"
]

[start]
cmd = "npx prisma migrate deploy || npx prisma db push --accept-data-loss; npx next start -p ${PORT:-3000}"
